I updated the README.md with all useful info about this project. I hope it helps with reviewing it.

I also double-checked the tests. I've been thinking of simplifying them or removing some of them that might be redundant, but after checking them I think there is no need to change anything about them.

Extra update: I renamed notes to use "-" instead of "#" because "#" wasn't working in the browser without passing it as "%23" and "%23" wasn't working locally in the code editor.